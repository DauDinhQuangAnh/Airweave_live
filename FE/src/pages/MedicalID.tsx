import MedicalIDDemo from './MedicalIDDemo';

// Re-export the existing Medical ID page so it lives at /medical-id inside the app shell.
const MedicalID = () => <MedicalIDDemo />;
export default MedicalID;
