# File này ĐƯỢC commit (không bị gitignore) → Vercel build production sẽ đọc.
# Chỉ chứa cấu hình CÔNG KHAI (URL BE), không đặt secret ở đây.
# BE đã deploy trên server obk-vm-ext-1 qua HTTPS (sslip.io).
VITE_API_URL="https://136-110-49-135.sslip.io/api"
